<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/public
    ServerName ${PARAM_REGISTRATION_URL}

    <Location />
        AuthType shibboleth
        ShibRequireSession Off
        Require shibboleth
    </Location>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined

</VirtualHost>
